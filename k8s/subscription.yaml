apiVersion: dapr.io/v1alpha1
kind: Subscription
metadata:
  name: orderservice-sub
  namespace: default
spec:
  topic: orders                 # must match the topic your publisher sends to
  route: /orders                # endpoint in orderservice that receives events
  pubsubname: order-pubsub        # must match your Dapr pubsub component name
